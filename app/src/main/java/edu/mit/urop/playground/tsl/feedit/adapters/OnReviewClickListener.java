package edu.mit.urop.playground.tsl.feedit.adapters;


/**
 * This is the interface through which the ReviewAdapter communicates the review taps with the DisplayReviewsActivity.
 *
 */

public interface OnReviewClickListener {

    void onShareButtonClicked(int reviewIdx);
}
